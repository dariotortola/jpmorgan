package com.jpmorgan.assignment.model;

/*
 * this enumeration have been written from known cases, but more values are
 * to be expected and included in a full application
 * 
 */
public enum StockType {
    COMMON, PREFERRED
}
